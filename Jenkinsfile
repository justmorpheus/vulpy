

testenv = "null"

pipeline {
            agent any
            stages { 
            stage('Installing packages') {
            steps {
                script {
                    sh 'ls -la'
                    sh 'pwd'
                }
            }
        }
            }
            
}
